'use client'
export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <p className="absolute top-0 left-0 z-50 grid w-full h-full bg-black center place-items-center">Загрузка...</p>
}